# 🚀 بناء المود على GitHub (بدون تنزيل أي برنامج)

كل العمل يتم عبر **المتصفح فقط** (Chrome, Edge, Firefox). لا تحتاج Git، ولا GitHub Desktop، ولا أي برنامج. جهازك يبقى نظيف.

> **الفكرة**: ترفع الكود الصغير (248KB) على GitHub عبر المتصفح → GitHub يبنيه أوتوماتيكياً بإنترنته السريع → تنزّل بس الـ JAR الجاهز (~1MB).

---

## ✅ كل ما تحتاجه

- حساب GitHub مجاني (إن لم يكن عندك): https://github.com/signup
- متصفح إنترنت

**انتهت المتطلبات.** لا تنزيل أي شي على جهازك.

---

## الخطوة 1: إنشاء repository (مستودع) جديد

1. افتح https://github.com/new (سجّل دخول أولاً)
2. املأ الحقول:
   - **Repository name**: `nuclear-eclipse-mod`
   - **Description** (اختياري): `Legendary nuclear bombs mod for Minecraft 1.21.1`
   - اختر **Public** (مجاني، والبناء مجاني للمشاريع العامة)
3. **مهم جداً**: اترك كل الخيارات الأخيرة **فارغة**:
   - ❌ لا تختر "Add a README file"
   - ❌ لا تختر ".gitignore"
   - ❌ لا تختر "license"
4. اضغط الزر الأخضر **Create repository**

ستفتح صفحة الـ repository الجديد الفارغ. اتركها مفتوحة.

---

## الخطوة 2: رفع ملفات المود

### الطريقة الأسرع — السحب والإفلات (drag-and-drop):

1. في صفحة الـ repository الفارغ، ابحث عن عبارة **"uploading an existing file"** (رابط أزرق) واضغط عليها
2. ستفتح صفرة رفع الملفات. افتح **مجلد `Minecraft Mod`** على جهازك في نافذة منفصلة
3. **حدّد كل المحتويات** داخل مجلد `Minecraft Mod` (اضغط Ctrl+A داخل المجلد)
4. **اسحب كل الملفات** إلى مربع الرمادي في صفحة GitHub (حيث مكتوب "Drag files here")

> ⚠️ اسحب **محتويات** المجلد وليس المجلد نفسه — نريد الملفات أن تظهر مباشرة في جذر الـ repository (مثل `build.gradle`, `src`, `gradlew`, `.github`, إلخ).

5. انتظر حتى يرفع كل الملفات (248KB فقط — سريع حتى على إنترنت ضعيف)
6. انزل للأسفل، اكتب في خانة **Commit changes**:
   - العنوان: `Initial commit — Nuclear Eclipse mod`
7. اضغط الزر الأخضر **Commit changes**

🎉 الآن كل ملفات المود أصبحت على GitHub!

### ملاحظة عن المجلد المخفي `.github`

مجلد `.github` (الذي يحتوي على الـ workflow) قد **لا يُرفع تلقائياً** عبر السحب لأن بعض الأنظمة تخفي المجلدات التي تبدأ بنقطة. لنتأكد:

1. في صفحة الـ repository، تأكد هل يوجد مجلد اسمه `.github` أم لا
2. **إذا وُجد** → ممتاز، انتقل للخطوة 3
3. **إذا لم يوجد** → سننشئه يدوياً (الخطوة 2-ب أدناه)

### الخطوة 2-ب: إنشاء مجلد `.github` يدوياً (فقط إذا لزم)

1. في صفحة الـ repository، اضغط زر **Add file** → **Create new file**
2. في خانة اسم الملف، اكتب بالضبط:
   ```
   .github/workflows/build.yml
   ```
   (عند كتابة `/` سيحوّلها GitHub إلى مجلد تلقائياً)
3. افتح ملف `.github/workflows/build.yml` الموجود على جهازك بالمفكرة (Notepad)
4. انسخ **كل محتواه** والصقه في مربع النص على GitHub
5. انزل للأسفل، اكتب رسالة commit: `Add build workflow`
6. اضغط **Commit new file**

---

## الخطوة 3: مراقبة البناء

بمجرد رفع ملف `.github/workflows/build.yml`، يبدأ البناء **تلقائياً**!

1. في صفحة الـ repository، اضغط تبويب **Actions** (في الأعلى)
2. ستجد workflow اسمه **"Build Nuclear Eclipse Mod"** مع دائرة صفراء تدور 🟡
3. اضغط عليه لرؤية التفاصيل
4. انتظر **5 إلى 15 دقيقة** (أول مرة أبطأ بسبب تنزيل Forge MDK ~200MB)
5. عند الانتهاء ستتحول الدائرة إلى:
   - ✅ أخضر = نجح
   - ❌ أحمر = فشل (انظر حل المشاكل بالأسفل)

---

## الخطوة 4: تنزيل الـ JAR الجاهز

عندما يصبح البناء **أخضر ✅**:

1. افتح الـ run الناجح (اضغط عليه في تبويب Actions)
2. انزل إلى آخر الصفحة لقسم **Artifacts**
3. ستجد ملفاً اسمه **`nucleareclipse-mod-jar`**
4. اضغط عليه — سيُنزّل ملف ZIP صغير (~1MB)
5. فك ضغط الـ ZIP (كليك يمين → Extract)
6. ستجد بداخله: **`nucleareclipse-1.0.0.jar`** 🎉

**هذا هو ملف المود الجاهز!** انسخه إلى مجلد `mods/` في تثبيت Minecraft Forge 1.21.1.

---

## 🔄 للتعديلات اللاحقة (بدون أي تنزيل أيضاً)

كل ما تحتاجه عند تعديل ملف:

1. افتح الملف على GitHub (اضغط عليه)
2. اضغط أيقونة القلم ✏️ (Edit)
3. عدّل المحتوى
4. اضغط **Commit changes**
5. البناء يعاد تلقائياً وتحصل على JAR جديد

لرفع ملف جديد بالكامل: **Add file → Upload files** → اسحب الملف.

---

## 🆘 حل المشاكل

### البناء أحمر ❌
1. اضغط على الـ run الفاشل
2. اضغط على **build** (في القائمة الجانبية)
3. انزل لرؤية الخطأ الأحمر في الـ logs
4. انزل لأسفل الصفحة → قسم **Artifacts** → نزّل **`build-logs`** لرؤية تفاصيل أكثر
5. انسخ لي نص الخطأ وسأساعدك

### "gradlew: Permission denied"
- غير موجود عادةً، لكن لو ظهر: أخبرني وسأعدّل الـ workflow

### البناء لم يبدأ أصلاً
- تأكد أن ملف `.github/workflows/build.yml` موجود بالضبط بهذا المسار
- اذهب لتبويب **Actions** → إذا طلب تفعيل، اضغط **I understand my workflows, go ahead and enable them**

### تريد تشغيل البناء يدوياً (بدون تعديل)
- تبويب **Actions** → اختر "Build Nuclear Eclipse Mod" من اليسار → اضغط **Run workflow** (زر أخضر أعلى اليمين)

---

## 📞 الخلاصة بالـ 5 خطوات

1. **أنشئ** repository عام جديد على github.com/new
2. **ارفع** محتويات مجلد `Minecraft Mod` عبر السحب (uploading an existing file)
3. **تأكد** من وجود `.github/workflows/build.yml` (أنشئه يدوياً إن لزم)
4. **راقب** البناء في تبويب Actions حتى يصبح أخضر ✅
5. **نزّل** الـ artifact `nucleareclipse-mod-jar` → حط الـ JAR في مجلد `mods/`

**ما نزّلت أي شي على جهازك.** 🎯
